<p id="notice"><%= notice %></p>

<h1>Listing Cloths</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Beacon identification key</th>
      <th>User</th>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cloths.each do |cloth| %>
      <tr>
        <td><%= cloth.name %></td>
        <td><%= cloth.beacon_identification_key %></td>
        <td><%= cloth.user %></td>
        <td><%= cloth.url %></td>
        <td><%= link_to 'Show', cloth %></td>
        <td><%= link_to 'Edit', edit_cloth_path(cloth) %></td>
        <td><%= link_to 'Destroy', cloth, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cloth', new_cloth_path %>
